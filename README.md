# 1. API 
```
1. drawImage(image, sx, sy, sWidth, sHeight, dx, dy, dWidth, dHeight)
  1.1 sx, sy开始裁切位置 sWidth sHeight 裁切长宽 dx, dy,放置位置 dWidth, dHeight 放置宽高
2. translate(x, y) //对Canvas坐标系进行整体位移
3. save() restore()// 类似栈 先进后出,后进先出

```

